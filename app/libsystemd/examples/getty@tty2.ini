# getty@tty2.ini - a second instance of the getty@.ini template.
#
# Inherits "description" and "exec" from getty@.ini, but overrides "args" to
# demonstrate that an instance can layer its own keys on top of the template.
args=--tty %i --baud 9600
