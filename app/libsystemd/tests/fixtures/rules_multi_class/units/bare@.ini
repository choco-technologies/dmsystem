# One of two templates used only by the rules_multi_class fixture, so a
# single device event resolving through two independent rule files can be
# proven to start (or stop) two different units, not just the same one
# twice. See rules/a.ini / rules/b.ini and
# notify_device_added_starts_every_rule_matching_the_same_class in
# libsystemd_test.c.
exec=dmbare
description=%v
