# Both "type" and "restart" have unrecognized values on purpose - test
# fixture for libsystemd_parse_service_type()/libsystemd_parse_restart_policy()
# falling back to their defaults (simple/no) with a warning, rather than
# failing the parse or silently enabling restart supervision.
exec=dmmetaunknown
type=forking
restart=on-success
