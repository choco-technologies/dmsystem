# Same as fixtures/template_only/bare@.ini, but one level down from the
# scanned units directory - regression fixture for on-demand instantiation
# resolving the template's actual (nested) directory instead of assuming it
# always sits directly under the scanned root.
exec=dmbare
description=%v
